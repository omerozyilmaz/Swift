import Foundation

class Kisiler {
    var kisiAd: String?
    var kisiId: Int?
    
    // Sınıfın başlatıcı metodu (initializer)
    init(kisiAd: String, kisiId: Int) {
        self.kisiAd = kisiAd
        self.kisiId = kisiId
    }
}
